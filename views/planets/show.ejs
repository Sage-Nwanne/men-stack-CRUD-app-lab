
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= planet.name %></title>
  </head>
  <body>
    <h1><%= planet.name %></h1>
    <% if ( planet.isARealPlanet) { %>
        <p>This is a real planet</p>
    <% } else { %>
        <p>This is a planet of your creation!</p>
   <% }%>
   <p>Description:<%=planet.description%></p>


   <form action="/planets/<%=planet._id%>?_method=DELETE" method="DELETE">
    <button type="submit">Delete Planet <%= planet.name %>?</button>
  </form>

<a href="/planets/<%= planet._id%>/edit">Edit<%planet.name %>?</a>
<br><br>
<a href="/planets/new"> Add another planet</a> 
<br><br>
<a href="/planets"> View your galaxy</a> 
  </body>
</html>
